---
import { hero } from '@config/leaderboard/hero';
---

<header class="relative overflow-hidden pt-28 md:pt-32 pb-8 px-6 md:px-8 max-w-7xl mx-auto">
  <div class="absolute -top-24 -right-24 w-96 h-96 bg-primary/10 rounded-full blur-[120px]"></div>
  <div class="relative z-10 flex flex-col md:flex-row justify-between items-start md:items-end gap-6">
    <div class="max-w-2xl">
      <!-- <div class="font-mono text-primary font-bold tracking-[0.2em] text-xs uppercase mb-4 px-3 py-1 bg-primary-container/30 w-fit rounded-full">
        {hero.badge}
      </div> -->
      <h1 class="text-5xl md:text-7xl font-black text-on-background tracking-tighter leading-none mb-6">
        {hero.titleLine1} <br />{hero.titleConnector} <span class="kinetic-gradient">{hero.titleLine2}</span>
      </h1>
      <p class="text-on-surface-variant text-lg leading-relaxed max-w-lg">
        {hero.description}
      </p>
    </div>
    <div class="md:text-right">
      <!-- <div class="font-mono text-sm text-on-surface-variant opac
       ity-60 mb-1">
        {hero.prizePoolLabel}
      </div> -->
      <!-- <div class="text-3xl md:text-4xl font-mono font-bold text-on-surface">
        {hero.prizePool}
      </div> -->
    </div>
  </div>
</header>
